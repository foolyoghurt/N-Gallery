<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <a class="navbar-brand" href="#"><%= config.name %></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
            <li class="active"><a href="/"><%= __('Home') %></a></li>
            <li id="del-selected" <%- is_writable ? '' : 'class="disabled"' %>>
                <a href="#" data-placement="bottom" data-content="Delete Success"><%= __('Delete Selected') %>
                    <span id="selected-badge" class="badge" style="margin-left: 10px">0</span>
                </a>
            </li>
            <li id="reverse-selected"><a href="#"><%= __('Reverse') %></a></li>
            <li id="all-selected"><a href="#"><%= __('Select All') %>
                    <span id="item-count-badge" class="badge" style="margin-left: 10px"><%= items.length %></span></a></li>
            <li id="cancel-selected"><a href="#"><%= __('Cancel All') %></a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right" style="padding-right: 10px">
            <li class="help">
                <a href="#"><i class="glyphicon glyphicon-question-sign"></i></a>
                <div class="dropdown-menu">
                    <%- help_doc %>
                </div>
            </li>
            <li><a id='zoom-out' href="#"><span class="glyphicon glyphicon-zoom-out"></span></a></li>
            <li><a id='zoom-in' href="#"><span class="glyphicon glyphicon-zoom-in"></span></a></li>

            <form  id="jump-to-item" class="navbar-form navbar-left" role="search">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="<%= __('(N:M)Jump to Nth page Mth pic') %>">
                </div>
                <button type="submit" class="btn btn-default"><%= __('Jump') %></button>
            </form>
        </ul>
    </div><!-- /.navbar-collapse -->
</nav>